\section{Testing}\label{sec:testing}

\subsection{Transcribing}
The transcription system using the Whisper model was tested using a variety of voice commands  
spoken by different people and in different environments.

%Eksempel tabel
\begin{table}
    \centering
    \begin{tabular}{l l l | l}
        \toprule
        \textbf{Spoken command} & \textbf{Transcribed command} & \textbf{Amount (Correct/Total)} & \textbf{Successful} \\
        \midrule
        Move forward & move forward &  & Y \\
        Turn left & turn left &  & Y \\
        Go back & go back &  & Y \\
        Stop & stop &  & Y \\
        Come here & come here &  & Y \\
        \bottomrule
    \end{tabular}
    \caption{Transcription test results}
    \label{tab:trancriptiontest}
\end{table}

The results are showed in \cref{tab:trancriptiontest}

\subsection{Command identification}
Small unit tests were created to test the command identification system. For each command different sentences were created to check the robustness of the process. The results are shown in \cref{tab:commandidentificationtest}.


%Eksempel tabel
\begin{table}
    \centering
    \begin{tabular}{l l | l}
        \toprule
        \textbf{Input string} & \textbf{Identified command} & \textbf{Successful} \\
        \midrule
        Please move forward 1 meter & move forward 1 meter & Y \\
        Can you turn left 90 degrees & turn left 1.57 radians & Y \\
        Go back 0.5 m & go back 0.5 meter & Y \\
        Stop right now & stop & Y \\
        Come here & come here & Y \\
        \bottomrule
    \end{tabular}
    \caption{Command identification test results}
    \label{tab:commandidentificationtest}
\end{table}

\subsection{Sound localisation}
The algorithm for finding where the command was said, was tested using real data from one of the 3 microphones. The expected output 

Before the algorithm:
This is the segment we know "come here" was said

after the algorithm:
the algorithm Successfully found the exact moment where "come" where said, which satisfyes out needs for later calculations with GGC_PHAT


\subsection{Movement control}
%striglt line plot m joint state
%striglt line plot m actual data







\subsection{Full system testing}

%tabel start posotion
\begin{table}
    \centering
    \begin{tabular}{l | l l}
        \toprule
        \textbf{movement command} & \textbf{actual position} & \textbf{diviation from (0,0)} \\
        \midrule

    +73 cm $\rightarrow$ -40 deg $\rightarrow$ +112 cm $\rightarrow$ return 3 & () & () \\

    +45 cm $\rightarrow$ +160 deg $\rightarrow$ +30 cm $\rightarrow$ -95 deg $\rightarrow$ return 4 & () & () \\

    +90 cm $\rightarrow$ -120 deg $\rightarrow$ +55 cm $\rightarrow$ +45 deg $\rightarrow$ +80 cm $\rightarrow$ return 5 & () & () \\

    +25 cm $\rightarrow$ +90 deg $\rightarrow$ +70 cm $\rightarrow$ -135 deg $\rightarrow$ +40 cm $\rightarrow$ +30 deg $\rightarrow$ return 6 & () & () \\

    +60 cm $\rightarrow$ -90 deg $\rightarrow$ +40 cm $\rightarrow$ +135 deg $\rightarrow$ +90 cm $\rightarrow$ -45 deg $\rightarrow$ +30 cm $\rightarrow$ return 7 & () & () \\


        \bottomrule
    \end{tabular}
    \caption{return}
    \label{tab:return}
\end{table}

% come here test


